---
type: discipline
semester: 5
name: "Востоковедение"
code: "Б1.ВМ1.1"
credits: 3
total_hours: 108
auditorium_hours: 32
self_study_hours: 76
difficulty: 3
priority: "medium"
status: active
control_form: "Зачёт"
department: "прочее"
exam_count: 0
---

# 🌏 Востоковедение

> [!info] Информация
> **Код:** Б1.ВМ1.1
> **ЗЕТ:** 3
> **Всего часов:** 108 (аудиторных: 32, СРС: 76)
> **Сложность:** 3/5 ⭐⭐⭐
> **Приоритет:** 🟡 Medium
> **Прогресс:** `= this.progress`%
> **Средний балл:** `= this.avg_grade`
> **Форма контроля:** Зачёт
> **Кафедра:** прочее

## 🎯 Цель дисциплины

TODO

## 🌐 Электронный курс

> [!warning] Электронный курс пока не добавлен
> Добавьте ссылку, когда она будет доступна.

**Ссылка:** 
**Структура курса:** 
**Дедлайны курса:** *автоматически ниже*

## 📚 Навигация

- [[Лекции]]
- [[Практики]]
- [[Лабораторные]]
- [[Темы]]
- [[Самостоятельная работа]]
- [[Контрольные]]
- [[Материалы]]

## 📊 Прогресс

```dataview
TABLE 
  length(filter(rows, (r) => r.status = "completed")) + "/" + length(rows) AS "Выполнено/Всего",
  round((length(filter(rows, (r) => r.status = "completed")) / length(rows)) * 100) + "%" AS "Прогресс"
FROM ""
WHERE (type = "lecture" OR type = "practice" OR type = "laboratory" OR type = "self-study" OR type = "control")
  AND discipline = this.file.name
GROUP BY true
```

## 📅 Ближайшие дедлайны

```dataview
TABLE 
  work_type AS "Тип",
  number AS "№",
  due AS "Дедлайн",
  status AS "Статус",
  date(due) - date(today) AS "Дней"
FROM ""
WHERE type = "deadline"
  AND discipline = this.file.name
  AND due >= date(today)
  AND status != "completed"
SORT due ASC
LIMIT 10
```

## 🚨 Просроченные задания

```dataview
TABLE 
  work_type AS "Тип",
  number AS "№",
  due AS "Дедлайн",
  status AS "Статус"
FROM ""
WHERE type = "deadline"
  AND discipline = this.file.name
  AND due < date(today)
  AND status != "completed"
SORT due ASC
```

## 🧠 Повторение (требует внимания сегодня)

```dataview
TABLE 
  review.next_review AS "След. повторение",
  review.interval AS "Интервал"
FROM ""
WHERE type = "topic"
  AND discipline = this.file.name
  AND review.next_review <= date(today)
  AND status != "completed"
SORT review.next_review ASC
```

## 📈 Оценки

```dataview
TABLE 
  work_type AS "Тип",
  number AS "№",
  grade AS "Оценка"
FROM ""
WHERE discipline = this.file.name
  AND grade
SORT file.name ASC
```

## ⚠️ Требует внимания

```dataview
TABLE
  work_type AS "Тип",
  number AS "№",
  status AS "Статус",
  due AS "Дедлайн",
  difficulty AS "Сложность"
FROM ""
WHERE discipline = this.file.name
  AND (type = "lecture" OR type = "practice" OR type = "laboratory" OR type = "self-study" OR type = "control")
  AND (status = "overdue" OR status = "not_started" AND due < date(today) OR difficulty >= 4 AND status != "completed")
SORT difficulty DESC, due ASC
```
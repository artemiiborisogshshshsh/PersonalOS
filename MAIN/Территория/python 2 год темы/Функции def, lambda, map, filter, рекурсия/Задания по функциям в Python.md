# Часть 1 — Обычные функции (дополненные задания)

## Лёгкий уровень (добавлены новые простые задачи)

### 1. Функция: вычисление финальной цены

Написать функцию `final_price(price, tax)`, которая возвращает итоговую цену товара с учётом налога.

### 1.1. Найти квадрат числа

Написать функцию `square(n)`, возвращающую квадрат числа.

### 1.2. Вернуть длину строки

Функция `str_len(s)` должна вернуть количество символов.

### 1.3. Проверить, чётное ли число

Функция `is_even(n)` возвращает True или False.

---

### 2. Функция: форматирование ФИО

Написать функцию `format_name(first, last)`, возвращающую строку вида:  
"Фамилия И.О."

### 2.1. Склеивание имени

Функция `hello(name)` возвращает строку `"Привет, <name>!"`.

---

### 3. Функция: проверка возраста

Написать функцию `is_adult(age)`, возвращающую True, если возраст ≥ 18.

### 3.1. Проверка доступа

Функция `can_play(age)`  
Если возраст ≥ 12 — вернуть `"Доступ разрешён"`, иначе `"Запрещено"`.

---

## Средний уровень (добавлены простые варианты)

### 4. Сравнение строк по частоте символов

Функция `same_letters(a, b)` проверяет, состоят ли слова из одинаковых букв.

### 4.1. Сравнение без регистра

Функция `same_ignore_case(a, b)` сравнивает строки без учёта регистра.

---

### 5. Нормализация списка

Функция `normalize(lst)` приводит элементы к диапазону 0..1.

### 5.1. Удаление отрицательных

Функция `remove_negative(lst)` удаляет числа меньше 0.

---

### 6. Объединение двух текстов

Функция `merge_text(t1, t2, sep="|")` — вернуть объединённый текст.

### 6.1. Повторение строки

Функция `repeat(s, n)` возвращает строку `s`, повторённую `n` раз.

---

## Сложный уровень (оставлены, добавлены чуть проще)

### 7. Анализ счёта пользователя

Функция `analyze_balance(operations)`, где операции вида ("add", 200)...

### 7.1. Безопасное вычисление

Функция `safe_add(a, b)` возвращает сумму, но если вход — не числа, вернуть `"Ошибка"`.

---

### 8. Функция-генератор отчёта по посещаемости

Полный расчёт процентов посещения.

### 8.1. Средняя оценка класса

Функция `avg_grade(grades)` — вернуть среднее арифметическое.

---

### 9. Многочлен любого порядка

Функция `poly(x, coeffs)`.

### 9.1. Упрощённая версия

Функция `poly2(x, a, b, c)` вычисляет квадратный многочлен `ax² + bx + c`.

---
# Часть 2 — Рекурсивные функции (добавлены лёгкие)

## Лёгкий уровень

### 10. Рекурсивный подсчёт гласных

Функция `rec_vowels(s)`.

### 10.1. Подсчёт цифр

`rec_digits(s)` возвращает количество цифр в строке.

---

### 11. Рекурсивное сравнение строк

Функция `rec_equal(a, b)`.

### 11.1. Рекурсивный подсчёт символов

`rec_len(s)` возвращает длину строки.

---

### 12. Рекурсивный поиск минимума

Функция `rec_min(lst)`.

### 12.1. Рекурсивная сумма списка

Функция `rec_sum(lst)`.

---

## Средний уровень

### 13. Рекурсивная сумма матрицы

`rec_mat_sum(matrix)`

### 13.1. Рекурсивный подсчёт чётных чисел

`rec_even_count(lst)`

---

### 14. Рекурсивное преобразование регистра

`rec_upper(s)`

### 14.1. Рекурсивное удаление пробелов

`rec_no_space(s)`

---

### 15. Рекурсивная развёртка словаря

`rec_keys(d)`

### 15.1. Рекурсивный вывод всех значений

`rec_values(d)`

---

## Сложный уровень

### 16. Рекурсивный анализ JSON-структуры

`json_count(data)`

### 16.1. Рекурсивный поиск ключа

`json_find(data, key)`

---

### 17. Рекурсивная фильтрация списков

`rec_filter(lst, cond)`

### 17.1. Рекурсивное удаление None

`rec_clean(lst)`

---

### 18. Рекурсивная сортировка (быстрая сортировка)

`qsort(lst)`

### 18.1. Рекурсивное разворачивание списка

`rec_reverse(lst)`

# Часть 3 — Lambda, map, filter (добавлены лёгкие)

## Лёгкий уровень

### 19. Удвоение чисел

map + lambda

### 19.1. Добавить 5 к каждому числу

map + lambda

---

### 20. Оставить слова длиной > 4

filter + lambda

### 20.1. Оставить только чётные числа

filter + lambda

---

### 21. Числа → строки

map

### 21.1. Строки → длины

map + lambda

---

## Средний уровень

### 22. Отсортировать список словарей

sorted(students, key=lambda ...)

### 22.1. Оставить только совершеннолетних

filter + lambda

---

### 23. Преобразовать матрицу в плоский список

map + lambda

### 23.1. Умножить все числа в матрице на 2

map + lambda

---

### 24. Оставить положительные числа

filter + lambda

### 24.1. Преобразовать строки в верхний регистр

map + lambda

---

## Сложный уровень

### 25. Комбинированная обработка оценок

map + filter + sorted

### 25.1. Очистка текстов от "плохих" слов

filter + lambda

---

### 26. map + filter для логов

"INFO:...", "WARN:..."

### 26.1. Вытащить только сообщение (убрать уровень)

map + lambda

---

### 27. Построить массив длин слов

map + lambda

### 27.1. Отфильтровать слова, начинающиеся на гласную

filter + lambda
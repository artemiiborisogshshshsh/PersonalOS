## **Вариант 1**

### **Тестовые**

1. При выполнении следующего кода на экране появится сумма двух чисел. Что именно будет выведено?
```python
print(2 + 3)
```
a) 23  
b) 5  
c) 2+3

2. Число 7 в Python относится к целым числам. Как называется такой тип?  
a) str  
b) int  
c) float
### **Практические**
3. Напиши программу, которая выводит текст **"привет"** на экран.

4. Напиши программу, которая складывает два числа, например 4 и 9, и выводит результат.

5. Напиши программу, которая сначала спрашивает у пользователя число, а потом выводит его на экран. Вводить будем с клавиатуры, а показывать через команду вывода.
<div class="page-break" style="page-break-before: always;"></div>
## **Вариант 2**
### **Тестовые**
1. Команда `print("Hello")` выводит текст на экран. Что именно она делает?
a) Сохраняет текст  
b) Печатает текст  
c) Удаляет текст

2. В Python комментарии начинаются с определённого символа, который ставится в начале строки. Какой это символ?
a) (
b) #  
c) <!-- -->
### **Практические**
3. Создай переменную с названием `age`, присвой ей своё количество лет и выведи на экран.
4. Напиши программу, которая спрашивает два числа у пользователя, складывает их и показывает результат. Числа должны обрабатываться как числа, а не как строки.
5. Выведи слово `Python`.
<div class="page-break" style="page-break-before: always;"></div>
## **Ответы**

**Вариант 1:**  
1 — b  
2 — b  
3 —

```python
print("Имя")
```

4 —

```python
print(4 + 9)
```

5 —

```python
num = input("Введите число: ")
print(num)
```

**Вариант 2:**  
1 — b  
2 — b  
3 —

```python
age = 15
print(age)
```

4 —

```python
a = int(input("Введите первое число: "))
b = int(input("Введите второе число: "))
print(a + b)
```

5 —

```python
print("Python " * 3)
```
